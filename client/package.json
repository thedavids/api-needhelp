{
  "name": "need-help-client",
  "version": "1.0.0",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "deploy": "npm run build && node deploy.js"
  },
  "devDependencies": {
    "dotenv": "^17.0.0",
    "ftp-deploy": "^2.4.7",
    "vite": "^5.0.0"
  }
}
